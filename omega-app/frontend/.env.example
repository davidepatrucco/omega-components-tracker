# Frontend Environment Variables Example
# Copy this file to .env.local and configure as needed

# API Base URL - Leave empty for development (uses Vite proxy)
# For production/staging builds, this is set by Docker build args
# VITE_API_URL=

# Examples:
# Development with local backend: VITE_API_URL=http://localhost:4000
# Development with staging backend: VITE_API_URL=https://api.staging.omega.intellitude.com
# Leave unset to use Vite proxy (recommended for local development)
